import Match from './match.js';

const personas = [{nombre: 'Juan', num:1, dato:'eldato'}, {nombre: 'Adolphite', num:2, dato:'eldato'}, {nombre: 'Albert', num:2, dato:'eldato'}]

const matchResult = Match(personas);

// console.log(matchResult);